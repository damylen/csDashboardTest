{
  "name": "root",
  "private": true,
  "dependencies": {
    "class-transformer": "^0.1.9",
    "date-fns": "^1.29.0"
  },
  "devDependencies": {
    "lerna": "^3.4.0"
  },
  "scripts": {
    "postinstall": "lerna bootstrap",
    "dev": "lerna run --parallel dev",
    "build": "lerna run build",
    "docker:build": "docker build -t dynamisch-draaiboek/haarlem-2018 ."
  }
}
